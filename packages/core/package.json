{
  "name": "@hiltwright/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure TypeScript domain logic for Hiltwright: config.h codec, blade model, presets.ini, board protocol. No Node, Electron or DOM imports.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
