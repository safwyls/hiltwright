# Hiltwright — Plan of Record

**Date:** 2026-09-07
**Status:** Path A, desktop-only, Electron + TypeScript. Committed.
**Baseline:** `hiltwright_bionic/docs/planning/proffie-configurator-planning.md` (the other agent's plan). This document keeps that plan's structure and decision and merges in the recon in [01](01-proffieos-recon.md), [02](02-toolchain-usb-recon.md), [03](03-ecosystem-and-users.md) plus the review changes listed in §0.

---

## 0. Changes from the baseline

| # | Change | Why |
|---|---|---|
| 1 | `@hiltwright/core` is a **pure TypeScript package** with zero Node, Electron or DOM imports. Main and renderer both import it. | Unit-testable flash and config logic; reviewable by the owner; not a web hedge. |
| 2 | The **board bridge runs in the renderer on Electron's Web Serial and WebUSB**, not `node-serialport` in main. Main keeps only subprocesses and files. | No native-module rebuilds per Electron upgrade; same pattern the Workbench proves; DFU stays a `dfu-util` subprocess. |
| 3 | **Runtime tier ships first** (presets, fonts, colours, arguments via `presets.ini`), build tier second. | Needs no toolchain and no Windows driver; useful to every existing Proffie owner on day one; de-risks the toolchain work. |
| 4 | **Flash-budget meter** added as a first-class feature. | `.text will not fit in region FLASH` is a top newbie failure, worst on 256 KB V2 boards. |
| 5 | **CFX / Xenopixel import** added (config, colours, font naming). | It is the on-ramp for the stated audience. |
| 6 | Factual fixes: no WebKitGTK on Electron/Linux; toolchain is ~300 MB download, ~1 GB unpacked, not multi-GB; ProffieConfig builds for Linux; compile time is **unmeasured**, not "tens of seconds"; canonical Workbench entry is fredrik.hubbe.net/lightsaber/webusb.html. | Accuracy. |

Everything else in the baseline stands.

---

## 1. Problem statement

ProffieOS is the most capable saber OS in the hobby, but its workflow was built for embedded enthusiasts: install Arduino, install the board core, hand-write a C++ header of defines, wiring structs and nested-template blade styles, point `ProffieOS.ino` at it, compile, deal with DFU drivers and compiler errors, upload.

The target user is not that enthusiast. They come from Crystal Focus X, Xenopixel and LGT boards, where configuration is a text file on an SD card, colours are numbers, and nothing is ever compiled. Hiltwright must deliver everything that makes Proffie exceptional while hiding the embedded toolchain entirely.

**Core constraint:** blade styles, blade wiring, the prop file and every `#define` are compile-time. Only preset order, font, track, name, variation and `RgbArg`/`IntArg` values are runtime data (via `presets.ini`). There is no architecture in which a brand-new style reaches the saber without a C++ compile. The only question is where the compiler runs. Decision: on the user's machine.

---

## 2. Technical recon summary

Full detail with citations in docs 01–03. The facts that shape the design:

- **Config anatomy.** One header included seven times with `CONFIG_TOP`, `CONFIG_STYLES`, `CONFIG_PRESETS`, `CONFIG_PROP`, `CONFIG_BUTTONS`, `PROP_BOTTOM`, `CONFIG_BOTTOM`. Preset = `{font, track, one StylePtr per blade, name}`. `BladeConfig` rows hold Blade ID resistance, LED count, pins, colour order, power pins, SubBlade splits.
- **Runtime layer.** `presets.ini` (+ `presets.tmp` backup) overrides the compiled preset array: `style=builtin P B <args>` references compiled styles with argument strings. Serial commands `list_presets`, `set_font`, `set_track`, `set_styleN`, `set_name`, `list_named_styles`; `RebootDFU`; `sd` with `MOUNT_SD_SETTING`. (`tag| command` framing exists in 8.10, with a space after the pipe; nothing tells an app which arguments a style uses; see 09.)
- **Build.** `arduino-cli compile --fqbn proffieboard:stm32l4:ProffieboardV3-L452RE:usb=cdc_msc_webusb,dosfs=sdmmc1,speed=80,opt=os`. Core v4.6 requires xPack `arm-none-eabi-gcc` 14.2.1 (357 MB download, 1.6 GB on disk). Compile time measured in [06-spike-results.md](06-spike-results.md): about 1 minute for a realistic Fett263 config on a fast desktop, expect 1 to 3 minutes on laptops; failures return in under 10 s. The artifact is `ProffieOS.ino.dfu` (raw image plus 16-byte DFU suffix).
- **Flash.** Running board is USB `1209:6668`. A 1200-baud touch (or `RebootDFU`, or BOOT+RESET) reboots it into the STM32 ROM bootloader `0483:df11`. Upload is `dfu-util -d 1209:6668,0483:df11 -a 0 -s 0x08000000:leave -D firmware.bin`. Recovery from a bad flash is BOOT+RESET; no software removes that.
- **Drivers.** Windows 10+: CDC serial and the WebUSB interface need nothing; the bootloader needs WinUSB once (`proffie-dfu-setup.exe`, libwdi, elevated). macOS: nothing. Linux: udev rules.
- **Flash budget.** V1/V2 = 256 KB (STM32L433CC), V3 = 512 KB (STM32L452RE).
- **Licensing.** ProffieOS GPL-3.0; Style Editor GPL-3.0 (forkable for previews); ProffieConfig GPL-3.0 (its `.pconf` prop definitions are reusable); Fett263 style code GPL with attribution, his web tools all-rights-reserved; fonts commercial and never redistributed.

Runtime vs compile:

| Thing | Runtime-editable? | Channel |
|---|---|---|
| Active preset, volume, effects, battery | Yes | serial / WebUSB |
| Preset add/delete/reorder, font, track, name, variation | Yes | `presets.ini` |
| Colours and options exposed as `RgbArg`/`IntArg` | Yes | `presets.ini` argument strings |
| Fonts and tracks on SD | Yes | mass storage / card reader |
| **Blade style structure** | **No** | recompile + reflash |
| Blade count, LED counts, wiring (`BladeConfig`) | No | recompile + reflash |
| Prop file, buttons, every `#define` | No | recompile + reflash |

---

## 3. Paths evaluated

Recorded in [04-paths.md](04-paths.md). Path A (desktop), Path B (web, no backend), Path C (web plus cloud compile). B cannot compile styles; C can, but only by making us run and pay for a build service forever, and neither can fix the Windows bootloader driver. Path A is the only path with full capability and no standing cost.

---

## 4. Decision

**Path A. Installed cross-platform desktop application (Windows, macOS, Linux). Electron + TypeScript. Committed, not hedged.** No servers, accounts, build queues or hosting. No web tier, and no architecture seams kept for one.

Electron rationale: the app's work is subprocess and file orchestration (`arduino-cli`, `dfu-util`, SD copies), not computation, and Node does that well. TypeScript keeps the whole codebase, including the flash pipeline, reviewable by the owner. One bundled Chromium on all three OSes protects the style editor, the most UI-sensitive surface, from platform WebView quirks. `electron-builder`, `electron-updater` and `@electron/notarize` are proven. Accepted cost: ~150–200 MB install, small next to the 1.6 GB toolchain cache. Tauri rejected as a Rust tax with no payoff here; Qt/wxWidgets rejected because the style editor needs fast UI iteration.

Simplifications that fall out: GPLv3 exposure reduces to ordinary tool aggregation with notices, because users compile their own configs and we never distribute compiled firmware. A local firmware cache (config hash → `.bin`) replaces any cloud cache.

Accepted sacrifices: zero-install tryout, phone-based tweaks, instant web distribution. Mitigated by an in-app updater and a curated style library shipped with the installer.

---

## 5. Architecture

### 5.1 Packages

**`@hiltwright/core` (pure TypeScript, no Node/Electron/DOM imports).** The single source of truth for domain logic, imported by both Electron processes and tested in isolation with golden fixtures.

- Config model ⇄ `config.h` codec. Round-trip safe: structured parse of the sections we understand, opaque-text preservation for everything else, so hand-written configs survive import.
- Style AST: parser, pretty-printer, validator per OS version, `RgbArg`/`IntArg` extraction, named-style resolution. Preview renderer forked from the GPL Style Editor lives in the UI package but consumes this AST.
- `presets.ini` codec, including `builtin P B <args>` and argument strings.
- Board protocol: command framing (`tag|command`), response parsing for `version`, `list_presets`, `list_named_style`, `describe_named_style`, `dir`, battery and volume.
- Hardware profiles: board variants, pin maps, LED chip tables, Blade ID conventions.
- Flash-budget estimator: empirical size table per style family and feature define, calibrated against real builds.
- Importers: CFX `config.txt`/`colors.txt`/`font_config.txt`, Xenopixel `SET/config.ini`/`fontconfig.ini`, font naming converter (CFX/GH/Xeno → Proffie).
- Prop definitions as data (seeded from ProffieConfig's GPL `.pconf` files and pod.hubbe.net).

**`@hiltwright/app` (Electron).**

Main process owns state, subprocesses and files:

- Toolchain manager: downloads and verifies pinned `arduino-cli`, Proffieboard core 4.x, xPack GCC 14.2.1 and pinned ProffieOS source trees on first use; per-version cache; integrity checks.
- Build engine: generates `config.h` from the model into a private copy of the pinned OS tree, sets `CONFIG_FILE`, runs `arduino-cli compile` with one build directory per saber, caches `sha256(config, OS version, FQBN)` → `.dfu` (the only cache that matters; the spike showed fresh rebuilds gain nothing from arduino-cli's own cache), and translates compiler errors into plain language using a config line map ("preset 4 has two looks but your saber has one blade"). Model-level validation (blade/style count, prop define conflicts, pin names) runs before any compile, since the compiler does not catch a valid-but-wrong pin name.
- Flash engine: backup first, reboot to bootloader (1200-baud touch via the renderer bridge, or `RebootDFU`), poll for `0483:df11` **and check its PnP driver service is WinUSB**, read the DfuSe memory layout to learn the flash size, run `dfu-util -d 1209:6668,0483:df11 -a 0 -s 0x08000000:leave -D <file>.dfu` as a subprocess (about a minute for 214 KB), wait for the serial port to return, verify with `version` (OS 8 reports config name, prop, buttons and build time), restore or merge `presets.ini`. Proven in the spike. Surfaces BOOT+RESET recovery instructions when the bootloader never appears.
- Driver helper (Windows): first-run "get my board flashable" wizard that runs the elevated WinUSB installer once (reuse `proffie-dfu-setup.exe` if its license allows bundling, otherwise libwdi directly).
- SD manager: detects the board's mass-storage volume or a card reader, validates fonts (mono/poly mixing, sample rate, `common/` folder), copies fonts and tracks, keeps a `ProffieOS source/` folder with the exact config on the card.
- Backup service: full-flash read-back (`dfu-util -U`) and `presets.ini` snapshots before any flash.
- Updater for the app, and a checker for new ProffieOS and core releases.

Renderer owns UI and the board bridge:

- UI: hardware wizard, presets, looks (style gallery and editor with live preview), fonts, diagnostics.
- Board bridge: Electron's built-in Web Serial and WebUSB, granted through `select-serial-port` / `select-usb-device` handlers in main. Speaks the core's protocol framing. Performs the 1200-baud touch on request from the flash engine.
- Strict typed IPC through a preload script; `contextIsolation` on; no `remote` module; renderer never spawns processes or touches the filesystem.

### 5.2 Data

- Local library in `userData`: sabers (model + `BladeConfig` identity), styles, backups, firmware cache.
- Every generated config carries a header comment with a JSON summary and content hash, and the same hash is written to the SD card, so a saber can always be re-identified.

### 5.3 Conventions

- **Golden baseline defines** in every generated config: `usb=cdc_msc_webusb`, `ENABLE_ALL_EDIT_OPTIONS` and the prop's edit-mode defines, `MOUNT_SD_SETTING`, `SAVE_STATE`, and **never** `DISABLE_DIAGNOSTIC_COMMANDS` (the spike showed it removes `list_named_style` and `id`, which the runtime tier uses). Live control and safe SD access are therefore always available.
- **`BladeConfig` is saber identity.** Imported or entered once through the wizard, verified against the hardware profile, shown read-only afterwards, and never silently regenerated. Wrong power pins can damage hardware.
- **Compile as little as possible.** Looks are parameterised styles; picking a look is usually a `presets.ini` edit, not a build. The flash meter is shown whenever a build is required.
- **Never brick, never lose.** No flash without a full backup and a `presets.ini` snapshot.

Diagram: [diagrams/committed-desktop.html](diagrams/committed-desktop.html).

---

## 6. Product tiers and sequencing

- **Tier 1, runtime editing.** Presets, fonts, tracks, order, names, colours and options exposed as arguments, variation, volume, diagnostics, backups. Works on any board already compiled with edit options; no toolchain, no Windows driver.
- **Tier 2, firmware build and install.** New styles, blade layout, prop, defines, OS upgrades. Needs the toolchain and, on Windows, the one-time driver.
- **Tier 3, SD content.** Fonts, tracks, `presets.ini` on the card.

### Roadmap

**Phase 0 — Core.** `@hiltwright/core` with golden tests against every config in the ProffieOS `config/` folder and community samples; style parser validated against the Style Editor's corpus; `presets.ini` round-trips; protocol client tested against recorded board transcripts.

> Status 2026-09-08: started. Driven by one vertical slice: plug in the V2.2, read its presets, change a preset's font, see the saber confirm it. Slice one (config codec + blade model) and slice two (`presets.ini` codec, protocol client and parsers, tested against transcripts recorded from a V2.2 on OS 7.8, see 09) are in `packages/core`. Slice three, the Electron walking skeleton (`apps/desktop`), connects to the same board over Electron's Web Serial with no click and shows version, battery and presets; see 09 for the permission-handler details. Slice four, the Presets page, edits the current preset on the board (name, font, common fallback, track) with a snapshot before each write and a read-back before anything is shown as saved; verified end to end on the V2.2. **The vertical slice is complete.**
>
> Tier 1 alpha, 2026-09-09: saber library in `userData` keyed by USB serial (Windows PnP) with config/install/blade-layout fallback; `presets.ini` snapshots on disk per saber; Presets page with reorder, duplicate, delete and a per-blade picker over compiled looks; `scanid` read into the saber identity; Fonts & SD via a card reader with WAV and naming checks and font copy; electron-builder packaging. Next: the second vertical slice, adopting a saber onto Hiltwright firmware (wizard identity → generated config → backup → build → flash → verify), wrapping the spike scripts as main-process services. See `docs/07-frontend-design.md` §7a for the blade model.

> Second vertical slice, 2026-09-09: adopt a saber onto Hiltwright firmware. `packages/core` gains `generateConfig` (a `SaberConfigModel` of board, buttons, prop, blades with roles and wiring, and the presets carried over, emitted as a config.h with Hiltwright starter looks and a hashed `hiltwright:` header). `apps/desktop` main gains the spike's toolchain, build and flash scripts as services (`toolchain.ts`, `build.ts` with error translation and a build cache keyed on config hash, `flash.ts` over dfu-util and Windows PnP) with a `job:event` progress stream, and the Build & Install page: wiring form (data pin and power pins per blade, guessed from the board and confirmed by the owner), prop choice, config preview, build with flash budget, and the install sequence backup → RebootDFU / 1200-baud touch → bootloader wait → write → wait for the runtime → reconnect. Verified on the V2.2: the generated config with the starter looks compiles to 83% of V2 flash in about 20 s from inside the app (`HILTWRIGHT_BUILD_E2E=1`, compile only). **Flashed for real the same evening**, on the owner's V2.2 (hote2, wiring confirmed from the installer's diagram): RebootDFU from the renderer put the board in its bootloader without the 1200-baud fallback, dfu-util read the 256 KB backup and wrote the 183 KB image (SA22C prop, 70% of flash), the board came back, and the app reconnected and read `config/hiltwright_hote2.h` with all 13 presets and the [140, 2, 1] blade layout intact. First attempt stopped safely at the write step: the "only Hiltwright-built firmware" guard compared paths with different slashes (fixed, and the page now also installs from a board already in bootloader mode). The OS 8 transcript is still wanted.

> Looks, 2026-09-10: `packages/core/src/looks.ts` defines looks (starter looks by role, pasted Fett263-library styles analysed for their edit-mode arguments and compiled defaults, the copyright header kept) and the firmware manifest a build produces (which look sits in which preset slot, with its argument numbers). `generateConfig` compiles any chosen look into any preset slot and returns the manifest; the app stores it on the saber record after a successful install, alongside the build model, so Build & Install no longer re-guesses wiring. The Looks page is the gallery (starters plus pasted looks in `userData/looks.json`), with compiled / queued / needs-build state per saber and "Add to saber" choosing the preset and blade slot to compile into. On the Presets page the look picker names Hiltwright looks and shows a colour swatch per argument, written live as `builtin P B <args>` and read back; verified on hote2 (base colour to red and back). A config carrying a pasted Fett263 fire style compiles (apps/desktop build test).

> Clean-machine adoption, 2026-09-13 (in progress): first-run toolchain install verified from an empty folder (`apps/desktop/test/toolchain.test.ts` with `HILTWRIGHT_INSTALL_TEST=1`: arduino-cli, core with GCC and dfu-util, ProffieOS v8.10; 47 s on a fast line, 2.0 GB on disk, then a compile from it). Found and fixed the Windows path-length trap (GCC's `cc1plus.exe` at 259 characters under a deep folder); the toolchain now lives under `%LOCALAPPDATA%\Hiltwright	oolchain` and roots over 120 characters are refused. Build & Install shows a one-time setup panel (sizes, folder, free space, progress, retry) even before a saber is connected, and a Windows driver wizard when the bootloader appears without WinUSB (open the ProffieOS setup page, run `proffie-dfu-setup.exe`, Check again, the install resumes from the backup). The driver wizard fired correctly on a clean PC on 2026-09-20 and was then made one click: `apps/desktop/src/main/driver.ts` downloads `proffie-dfu-setup.exe` from fredrik.hubbe.net into the owner's data folder, checks its size (and SHA-256 once pinned), launches it elevated, waits for WinUSB to bind and resumes the install; the manual route stays as a link. **Verified end to end on a clean Windows PC, 2026-09-20:** clone, `npm install`, `npm run dev`, toolchain installed by the app into `%ProgramData%/Hiltwright`, saber connected with no click, build, one-click driver install through the Windows approval prompt, automatic resume through backup, write and reconnect; the board came back reporting `v8.10 hiltwright ce12a06`, `config/hiltwright_hote2.h`, 13 presets and the [140, 2, 1] blades. The installer's SHA-256 is pinned from that run. The count-first hardware editor (`apps/desktop/src/renderer/src/Hardware.tsx`) replaced the guessed pin form: blade count stepper, one card per blade with role, what it is built from (pixel strip, single LED, star LED, motor), pixels and colour order, wiring as "own data line" or "continues Blade N's wire" (reversed under the chain), power pins or the FET/PWM pin per LED, plus a live diagram and pin table with conflicts and shared-power notes. It produces core `ModelBlade`s directly; the generator test covers a chained reversed crystal, a single LED on a V3 Free pin, a tri-colour star and a motor. Blade ID variants followed on 2026-09-20: the main blade card asks "Do you swap blades?"; each blade gets a name, a pixel count and a Measure button that reads `BLADE ID:` from `scanid` (never typed), plus an optional empty-emitter row (`NO_BLADE`). Turning it on adds `ENABLE_POWER_FOR_ID`, `BLADE_ID_SCAN_MILLIS`, `BLADE_ID_TIMES`, `BLADE_ID_STOP_SCAN_WHILE_IGNITED` and `SHARED_POWER_PINS`; the generator emits one `BladeConfig` row per measured blade and refuses readings closer than 15%. Because a reading only matches when it is taken by firmware that powers the blade while measuring, measuring is offered only after one install with the option on; the rows go in with the second install. Compiles at 70% of V2 flash; **untested on hardware**. Build & Install also works from a remembered saber with nothing plugged in (wiring, looks and a build need no board; installing does), and saved wiring only counts as confirmed once it has been installed. Backups can be put back from the same page: they are listed per saber, checked to start like real firmware for this chip (stack pointer in SRAM, reset vector in flash, so an erased or protected read is refused), written with the same bootloader sequence, and the firmware manifest is cleared afterwards because the restored firmware is not Hiltwright's. Verified on hardware by the owner on 2026-09-20, along with the Diagnostics sliders and the voice pack check; Blade ID is shelved until a saber with an ID resistor setup is available. The Xenopixel half of the import is built (Fonts & SD: choose the old card, convert fonts onto the ProffieOS card with ProffieOS names, add a preset per font with the old colour); tested against a fabricated card, not a real one. CFX waits for a real card or its manual; see 03. A look library followed (2026-09-20): fourteen original Hiltwright looks in `packages/core/src/lookLibrary.ts` (nine for blades: Steady, Humming, Pulsing, Unstable, Fire, Energy flow, Swing flare, Hot tip, Rainbow; four for crystals and accents: Follow the blade, Living crystal, Flash on hits, Battery gauge; one motor). Blade looks share `HwFx`, a wrapper adding blast, responsive clash, lockup, lightning block, drag and stab plus wipe in and out, with every colour and both timings as edit-mode arguments. Each look's argument list and default colours are scanned from its own C++, a build only carries the looks it uses, and `apps/desktop/test/lookSizes.test.ts` compiles every look with the real toolchain: all compile, the baseline is 70% of V2 flash, and each extra look costs between 0.6 and 1.5 KB. Not yet seen on a blade. A simulated preview followed the same day. `packages/core/src/sim.ts` ports the per-LED maths of every ProffieOS building block the library uses (same 0..32768 fixed point, same sine and hump tables, same wipe, bump, blast-ring, stripes and fire-column algorithms) and runs each library look against the preset's real style arguments; sound level and motion are modelled stand-ins, blade angle is fixed at horizontal. Because it is derived from ProffieOS's GPL-3.0 style headers, the simulator is GPL-3.0 like the rest of Hiltwright. `apps/desktop` draws the LED values with PixiJS 8 (`bladeRender.ts`): one shared WebGL renderer copies into plain 2D canvases (the gallery shows fourteen blades and browsers cap WebGL contexts), and the shader models what makes a blade differ from its RGB numbers: no gamma in the firmware, diffuser smear in linear light, channel saturation, and a two-scale glow. It needs `pixi.js/unsafe-eval` because of the app's CSP. The preview is on the Looks page (gallery and detail, with ignite, clash, blast, stab, lockup, drag, lightning and a swing slider) and on Presets per blade, at the blade's real LED count, following the colour picker while it is dragged. Pasted looks cannot be simulated and keep the flat bar. All fifteen previews cost about 2 ms per frame. Not yet compared against a real blade. A layout revision pass followed (2026-09-20), after the owner found actions hard to reach and pages too long. Rule: every page is a workspace that fits the window (checked at the 1280x760 minimum); panels scroll, pages do not. The page title moved into the top bar, and eyebrows, lead paragraphs and the footer status bar went. Build & Install is now a tabbed workspace (Wiring, Config, Log, Backups) beside an always-visible rail of the four real steps (wiring confirmed, compiler, build, install); the rail highlights the next step and holds its one button, the driver wizard and notes live in it, and the Log tab opens by itself when work starts. Looks has one toolbar, a paste dialog instead of a panel that pushed the gallery down, and the add-to-saber controls directly under the preview. Presets keeps list and editor and gains a right column with Try it (effect triggers, volume, brightness, shared with Diagnostics) and History. Armory shows presets one line each with look names, quick actions, and offers looks and builds for a remembered saber while unplugged. The nav shows how many looks are queued for a build; Ctrl+1 to 6 switch pages. Dev aids added: `HILTWRIGHT_OFFSCREEN=1` (with `HILTWRIGHT_W`/`HILTWRIGHT_H`) renders without a display so `HILTWRIGHT_SHOT` works with the screen asleep, and `HILTWRIGHT_PAGE=fake:<page>` shows the connected pages from the remembered saber with no board (dev builds only). The library then grew to twenty-five looks (2026-09-20). The Fett263 style library was surveyed for its range of ideas only (category and option names on its public page; none of its style code was read or reused), and eleven original looks were composed from ProffieOS primitives: Film flicker, Power surge, Stardust, Lava lamp, Emitter flare, Aurora, Current (flows faster in a swing), Dark blade, and for crystals and accent strips Heartbeat, Scanner and Sound meter. The simulator gained SparkleF, RandomF, function-driven Stripes and TransitionLoopL with TrConcat, fades and delays. All eleven compile on the real toolchain at 0.7 to 2.0 KB each. `HILTWRIGHT_LOOK_ONLY=hw_a,hw_b` limits the size test to named looks. Still to come from that survey: ignition and retraction variants (spark tip, centre out), which need a second effects wrapper. A further twelve looks followed (2026-09-21), for thirty-seven: Embers, Horizon (colour follows blade tilt), Core pulse, Tracers, Split, Barber pole, Static, Resonance, Tip comet, Interference, and the two ignition variants deferred earlier, Spark ignition and Unfold (centre out). The shared wrapper became `HwFxTr<BASE, OUT_TR, IN_TR>` with `HwFx<BASE>` as the usual wipe on top of it, at no flash cost. The simulator gained Saw, HumpFlickerF, BladeAngle (with a Tilt slider in the preview), centre wipes and the spark that rides a wipe. All twelve compile at 0.6 to 1.5 KB. Motion followed (2026-09-21). ProffieOS exposes three motion inputs to styles: BladeAngle (tilt, from gravity), SwingSpeed and TwistAngle (from the gyro and fusion). Swing flare, Current, Split and Tip comet already read swing and Horizon reads tilt; three looks were added that are about gravity itself: Liquid (the second colour pools at the lower end), Downhill (bands run downhill, faster the steeper) and Twist dial. The shared effects were already tilt-driven in the firmware, since ResponsiveClashL and ResponsiveLockupL place the hit from BladeAngle, and the simulator now does the same instead of using a fixed spot. The preview gained a motion pad: a saber that is dragged to tilt it, with drag speed read as swing speed and decaying on release, beside Swing, Tilt and Twist sliders that set the same values. The Looks toolbar has a Motion filter. Forty looks; the three new ones compile at about 1 KB each. Colours chosen on the Looks page now travel with the look (2026-09-21). A look already on the saber is written straight to the chosen preset and blade with those arguments (`board.setPresetStyles`: select the preset if needed, write, read back). A look that still needs a build keeps its arguments beside the slot in the model (`presets[].lookArgs`, ignored by the generator so the firmware hash does not change); once the saber is connected with firmware whose manifest has that look in that slot, `usePendingLookColours` points the preset at `builtin <preset> <blade> <args>`, confirms by read-back, returns to the preset that was selected, and drops them. This also makes 'Add to preset' true for the first time: before, the owner still had to pick the new look on Presets after the install. The apply controls are pinned to the foot of the look panel. Not yet run against hardware. A demo room followed (2026-09-21): a page with a saber in a dark 3D space, wielded with the mouse. three.js 0.170 draws the room, hilt and blade (`demoScene.ts`, loaded lazily so it costs nothing until the page is opened; PixiJS still draws the 2D previews). The blade is a tube textured one texel per LED from the same `BladeSim`, three point lights along it colour the floor, and only the blade blooms: the scene is rendered once with everything else blacked out for the glow and once as it is, then added, because blooming the whole frame made the lit hilt glow and a luma threshold would cut out blue blades. The cursor aims the blade on a sphere around the hand, the blade follows with a little weight, and what it actually did is fed to the simulator as the sensors would report it: tilt from the blade's elevation, swing from its angular speed, twist from the scroll wheel. Drag swings, scroll twists, double-click or Space ignites, clicking the blade lands a blast there, C/B/S and L/D/N trigger and hold effects, right-drag orbits. A look dropdown and its colour swatches sit in a corner panel, and the Looks page opens a look there. Verified by offscreen screenshots with synthetic pointer events; not yet driven by hand. The demo room's control was then turned round (2026-09-21) after the owner found it backwards: the cursor had been aiming the tip. Now the drag moves the hand that holds the hilt (relative to where it was grabbed, so nothing jumps), and `wield.ts`, pure and tested, moves the blade: a rod with its mass at the tip, pulled by the wrist toward pointing away from the chest, so a raised hand points it up and a lowered one down and out; under-damped, so a quick move makes the blade trail, whip through at several hundred degrees a second and follow through before settling. The demo room then gained a second mouse control, now the default (2026-09-21), because in hold-the-hilt a left-right drag arced the blade vertically: that model has no yaw axis, only a direction away from the chest. 'Tilt and swing' (`steer.ts`, pure and tested) keeps the hand still: left and right turn the blade about the vertical, so the tip sweeps an arc parallel to the floor at constant tilt, and up and down change the tilt only, clamped short of vertical. 'Hold the hilt' is kept as the alternative; the choice is remembered. The room opens on the owner's chosen pose and view (blade to the right and a little away, 42 degrees up, camera close, to one side and above). Middle-drag pans, Ctrl+scroll or +/- zooms, right-drag orbits, R resets all of it. Hold-the-hilt was then corrected (2026-09-21): a left-to-right sweep went up and over because the chest the blade points away from had been placed beyond the hand, as if the wielder faced the viewer. The wielder is the viewer: the chest is now on the camera's side, so the sweep carries the blade through forward, level with the floor (tested: it points almost straight into the room mid-sweep and never rises more than a few degrees). The hand also follows an arm's arc, furthest forward at the middle (`handOnArc`), and is placed by how far across and how high the cursor is rather than by a 3D point. Each mode has its own opening pose and view, from above and behind for this one, and switching mode resets to it. Hold the hilt became the demo room's default at the owner's word, and the room gained a Scene panel in the bottom right (2026-09-21): glow strength and spread (the bloom pass), blade brightness (how far past white the blade is drawn), how strongly the blade lights the floor and hilt, the room's own lamps, haze, and the floor grid, with a reset; collapsible, and remembered between sessions along with the control mode. Custom hilts for the demo room followed (2026-09-21). The Scene panel loads a `.glb` (materials and textures kept), `.obj` or `.stl` (given a steel finish) through an ordinary file input; `hiltModel.ts` takes the model's longest dimension as the saber's axis, stands it up, centres it, guesses its units from its length (a hilt is 15 to 50 cm whether the file is in metres, centimetres, millimetres or inches) and seats the blade end where the blade starts, with owner controls for length, turning it about the blade, and which end the blade comes out of. Models and their fit are kept in IndexedDB (`hiltStore.ts`) and the chosen one is remembered. The page's CSP gained `blob:` for images and fetches, which is how a loaded model's textures arrive. Fitting and STL/OBJ parsing are tested under node, and an STL was loaded end to end through the file input; no real GLB has been tried yet, and Draco- or meshopt-compressed GLBs and multi-file `.gltf` are not supported. OBJ hilts then gained their .mtl (2026-09-21): the file picker takes several files, the .mtl the OBJ names (and any textures it names) are kept as side files with the model and served to the loader as blob URLs, and each Phong material is turned into metal/roughness (bright specular means metal, shininess sets roughness). Verified end to end with a textured OBJ through the picker. This is for Fusion 360 exports; the owner was also told how to get a textured GLB out of Blender or Marmoset Toolbag. The Scene panel then gained the blade itself (2026-09-21): length in inches (20 to 40) and a 1 inch or 7/8 inch tube. Changing either rebuilds the tube and tip, moves the lamps along it and gives the wrist model the new length, so a short blade whips faster in the hand. The LED count now follows from the blade (2026-09-21): length times strip density (60, 100, 120 or 144 per metre, chosen in the panel; 144 is the default, giving 132 for a 36 inch blade), and the simulator is rebuilt with that many LEDs when either changes. A pattern therefore keeps its physical size on a shorter blade instead of being squeezed, which is what the owner wanted to see. A wobble the owner saw when turning an OBJ hilt was traced to centring the model by its bounding box (2026-09-21): a control box on one side puts the box centre off the bore, and Turn then orbits the body round the blade. The fitter now takes the file's own origin as the axis whenever it runs through the model, which is how CAD hilts are drawn, and only falls back to the box centre for a file modelled off in space; a panel switch forces either. A test reproduces the wobble under box-centring and shows the body on the axis at every turn under the fix. The saber's card over USB followed (2026-09-21). Every Hiltwright build already carried the mass-storage interface (`usb=cdc_msc_webusb`), but OS 8's `MOUNT_SD_SETTING` keeps it ejected until asked. Fonts & SD now has 'Share the saber's card': it sends `sd 1` (ProffieOS turns the blade off, stops idle sound, releases the card, and confirms 'SD Access ON'), waits up to 20 s for a new removable volume, and then runs the ordinary card-reader code on it. 'Give the card back' first has Windows eject the volume (Shell.Application 'Eject', which flushes and dismounts; refuses if something holds a file open) and then sends `sd 0`. Vendor firmware without the setting answers `Whut?` and the page says so. Untested on hardware: the whole flow, and how slow SPI copying is on a V2. The .hwpack format followed (2026-09-21), so creators' hilts and fonts can ship inside Hiltwright without their files being liftable. `apps/desktop/src/main/hwpack.ts`: a readable JSON manifest (id, kind, name, creator, licence, what the app may do with it, a hilt's fit) followed by items each deflated and AES-256-GCM encrypted under a key derived from the app key and the pack id; main decrypts into memory and never writes a file. A hilt is packed as a display mesh (OBJ and MTL parsed at pack time into positions, normals, indices and one material group per body, with metal or paint guessed from the CAD colour), not the CAD export. `scripts/hwpack.ts` (`node scripts/hwpack.ts hilt|font|show`, Node 24 strips the types) makes packs; packs are read from userData/packs, the app's resources/packs (the repo's `packs/` in development, gitignored) and `HILTWRIGHT_PACKS`. The demo room lists pack hilts with credits and the pack's fit as defaults (owner adjustments kept in IndexedDB, Reset fit returns to the pack's; Copy fit puts the numbers on the clipboard for whoever packs) and pack fonts as a 'Hiltwright fonts' source. First packs: five of the owner's hilts (Exar Kun, YS, Elzar Mann, OWK TPM, Mara Redux) with fits the owner set by hand, and the six free ProffieOS fonts (CC BY-SA 4.0, copying to a card allowed). What this protects against is stated plainly in the format's header: casual copying, not a debugger. A sixth hilt pack (Punk Saber) and a staff option followed (2026-09-21): the Scene panel's 'Second blade (staff)' hangs a second blade from the pommel, wherever the fitted hilt puts it, showing the same LEDs as the first, since a staff preset drives both blades alike, with its own lamps for the floor. Built for a double-bladed hilt of the owner's; neither of us is sure it earns its place. The owner liked the staff, so it was finished (2026-09-21): the second blade runs its own simulator, LED strip and look, chosen on a Main blade / Staff blade tab strip in the demo room's panel with its own colours; every effect and lockup reaches both blades, and the staff blade's tilt is the negative of the main one's since it points the other way. 'Staff seat' in the hilt fit sets how deep the second blade sits in the pommel, baked into packs like the rest of the fit. 'As on the saber' takes the staff blade's look from the preset's second main or side blade. A style editor followed (2026-09-21), the owner having found the official ProffieOS style editor (a C++ template tree) too much for non-technical owners. Hiltwright's is a stack of plain-language blocks: a base pattern (Steady, Pulsing, Humming, Unstable, Fire, Energy flow, Two-tone, Rainbow, Lava lamp, Horizon), effect layers added, reordered and removed (Blaster deflect, Clash, Lockup, Drag, Lightning block, Stab, Swing flare, Emitter flare, Stardust, Core pulse, Hot tip), and an ignition and retraction each (Wipe, Spark tip, From the middle, Fade, Instant). Each block has a name, a description, a few sliders and colour swatches. `packages/core/src/styleBuilder.ts` holds every block with its C++ (from the same templates as the library) and its simulator (from the simulator's own primitives, now exported as `prims`) side by side, so the live preview is the firmware's maths and the code cannot drift from it. Colours are standard edit-mode arguments, so a built look's colours change live on the saber. Saving makes a look (`source: 'built'`, with its alias define and its layers kept for re-editing) in the looks store; it appears in Looks with an 'Edit the layers' button, can be put on a preset and compiled like any library look, and previews everywhere because saved styles register a simulator at startup. A style with every effect layer, the lava base, spark ignition and centre retraction compiled on the real toolchain at 70% of V2 flash. The generated code can be shown for the curious. Not yet: choosing which effect an effect layer answers to, drag-and-drop reordering, and the long tail of the official editor's functions. > Full style editor, 2026-09-21: the owner tried the block editor and asked for the reach of the official ProffieOS style editor ('it lets you do all kinds of stuff using Layers<>'), so the editor now has two modes. 'Simple' is the block stack. 'Full' works on the style as a tree, the same shape the official editor and every style library use: paste any `StylePtr<...>()` or bare expression (`packages/core/src/styleTree.ts`, parser and printer; the comment header is dropped, empty `<>` preserved), see it as a Blade card and a stack of layer cards, and edit every slot: colours as swatches, `RgbArg` as a live-setting picker plus starting colour, numbers as fields, effects and lockup types as menus, everything else as a nested card with a menu of every template of the right kind. What each template takes, what it returns, its defaults and its documentation come from a catalogue generated from the ProffieOS source (`apps/desktop/scripts/catalogue.ts` scans `styles/`, `functions/` and `transitions/` for templates, aliases and `// Usage:` docs; 443 entries, 268 documented, into `packages/core/src/proffieCatalogue.ts`; regenerate when the pinned OS changes). Slots left at their default show the default and a Set button; variadic templates (Layers, Mix, Stripes, TrConcat) grow and shrink. Swapping a template keeps arguments whose kinds still fit. The preview runs the tree on the simulator through an evaluator over the exported `prims` (`evaluateStyle`): Layers, AlphaL, Mix, colours, the Responsive layers with their exact ProffieOS parameter orders, LockupTrL, TransitionEffectL, TransitionLoopL, InOutTrL, the Tr family incl. X-variants and spark tips, and the common functions (Sin, Saw, Scale, Bump, SmoothStep, SwingSpeed, BladeAngle, TwistAngle, sound, battery, noise, sparkle, Ifon, Sum, Mult, ...). Templates it does not model are skipped (a layer paints nothing, a function passes its first argument) and named in an amber note over the preview, so the preview is honest. A simple look can be opened as a tree at any time (`treeFromCpp` on its generated C++); a tree saves as a `built` look with `style.version: 2` and previews everywhere like any other. The owner's example (Red with every Responsive layer) parses, round-trips, runs with nothing unsupported and compiled on the real toolchain at 70% of V2 flash in 16 s; five other real-world styles (spark-tip wipes, StyleFire with FireConfig, Stripes, Rainbow under SwingSpeed, edit-mode RgbArgs with IgnitionTime) run with only cosmetic templates unmodelled. Not yet: drag-and-drop reordering, undo, a search-by-effect for the 'Add a layer' list beyond the text filter, and the evaluator's long tail (RotateColorsX, FireConfig tuning, EffectSequence, the blaster and detonator templates).

> Full style editor, validated against a library style, 2026-09-21 (later): the owner pasted Fett263's Acolyte (a 5 KB single-line style: AudioFlicker over Stripes, a random blast wave, an impact-dependent Mix of two clash layers, a lockup built from TransitionEffect/AlphaMixL/TrExtend/TrJoin/TrDelay, a strobing lightning block, sparking drag, a hue-rotated melt, and bent ignition and retraction times) to validate the builder and the simulator. It parsed and printed back byte for byte (the parser now also drops the trailing comma a presets array leaves), but the first evaluator showed a black blade: Mix of two layers had been flattened to an opaque colour, and TransitionEffectL only understood TrConcat. The evaluator was rebuilt to the firmware's own shape rather than special cases: (1) the catalogue now records each `using` alias's body (148 of them) and the evaluator expands aliases by substituting arguments and parameter defaults, so ResponsiveClashL, TransitionEffect, Strobe, TrWipeSparkTip, IgnitionTime and the rest behave exactly as the compiler builds them and need no code; only primitive classes are written out. (2) Transitions have begin/run/done/getColor(a, b, led) as transitions/base.h defines them: Instant, Fade, SmoothFade, Delay, Wipe, WipeIn, CenterWipe(In), WaveX, SparkX, Concat, Join(R), Extend, Blink, Boing, Loop, LoopN, Random, Select, Sequence, DoEffect. (3) Layers carry alpha throughout (Layers with a layer base stays a layer; Mix and AlphaMixL blend alpha; `<<` compositing as the firmware's), and LockupTrL, InOutTrL, TransitionEffectL (with the Multi variant), TransitionLoopL and TransitionPulseL are the firmware's state machines. (4) Effects are detected from the simulator's state: CLASH/BLAST/STAB from the effect list, LOCKUP/DRAG begin and end from lockup changes, IGNITION/RETRACTION from the power state; EffectPosition and EffectRandomF follow them; IntArg reads the preset's arguments so RetractionTime<0> falls back to the sound length as on the saber. (5) Melt is now its own lockup type in the simulator and has a button in the preview and the demo room (fonts play drag sounds for it, as ProffieOS does), since library styles keep drag and melt distinct. Acolyte now ignites, idles, answers every effect and retracts on the simulator with nothing unsupported, and compiled on the real toolchain at 73% of V2 flash in 17 s with all 14 of its live settings found. The permanent test uses a style of the same shape written for the purpose rather than the library's code. Also fixed: TrConcat's slots alternate transition/colour and TrJoin's are transitions in the editor; 'Or:' alias lines no longer leak into docs (284 documented). Still to do: the long tail of rarely used templates (EffectSequence, blaster and detonator styles, TrColorCycle), drag-and-drop reordering, undo.

> Demo room takes any saved look, 2026-09-21: the look picker lists Hiltwright's looks and, under 'Your looks', every saved look the simulator can run. `registerLookSim` (`packages/core/src/lookSim.ts`) gives a saved look a simulator from its layers (built, either editor) or, for a pasted look, by parsing its code through the tree evaluator, so library pastes swing in the demo room and preview on Looks and Presets too; it runs at startup, when Looks loads and when a paste is added. A pasted style that the evaluator cannot parse simply stays out of the list. Melt has its M key in the demo room's hints.

> The simulator as the saber, 2026-09-22: the owner pasted the Acolyte variant with Fett263's 'Cortosis Clash V2' special ability (a clash within 3 s of the ability shorts the blade out, sparks at the emitter for 10 s, then a partial 'flame up' and a full re-ignition) and asked that a style loaded into Hiltwright demo exactly as it would on the board. That style does not just paint: it steers the prop. TrDoEffect raises EFFECT_FAST_OFF, and the Fett263 prop turns the saber off; ten seconds later EFFECT_FAST_ON turns it back on; EFFECT_TRANSITION_SOUND asks for trNN.wav; the ability itself is EFFECT_USER1. So the simulator grew an effect bus (`SimEffect` with type, position, wavnum, sequence number and clash strength; `BladeSim.doEffect/raise/onEffect`) and the prop's reactions to the effects styles use to drive it (FAST_OFF/OFF turn it off, FAST_ON/ON turn it on, ignition/retraction and lockup/drag begin/end are put on the bus as they happen). Detectors in the evaluator read the bus the way OneshotEffectDetector does (newest match, once; a clash detector also answers stabs unless the style handles stabs). The remaining templates were ported from the source: EffectPulseF, HoldPeakF, ThresholdPulseF, IncrementModuloF, IncrementWithReset, LockupPulseF, Trigger, TimeSinceEffect, ChangeSlowly, OnSparkF, BlinkingF, RandomBlinkF, SequenceF, RampF, LinearSectionF, CircularSectionF, IntSelect(X), ModF, ClampF(X), WavNum, BlastF, BlastFadeoutF, EffectSequence, ColorSequence, ColorCycle, LockupL (which steps aside for lockup types the style's LockupTrLs handle), LocalizedClashL, Remap, PixelateX, TransitionLoopWhileL, TrLoopUntil, TrDoEffectX and TrDoEffectAlwaysX; and TrConcat now follows the firmware's real rule (consecutive transitions without a colour between them both see the outer A and B, as TrConcat2 nests). ClashImpactF reads a real strength: a plain clash is 5 g, a swing behind it adds up to 16 g, and there is a 'Hard clash' button, so Real Clash V1's soft and hard branches both show. The evaluator's report now separates 'unsupported' (not shown) from 'approximate' (shown without the sensors: clash strength, sound level, sound lengths, swing and twist acceleration, MarbleF), and the editor says so under the preview. Every template a blade style can name is modelled except SliceF (the smoke shader); catalogue internals (SFINAE finders, POV and display styles, test types) are flagged so the pickers never offer them. The demo room and the 2D preview get Ability 1-4 (keys 1-4) and Hard clash, the preview's Ignite/Retract follows what the style did to the prop, the demo scene takes on/off, effects and sounds off the main blade's bus (so a style-driven retraction plays the font's retraction), and the font reader and engine know trNN.wav and tr/NNN/000.wav for EFFECT_TRANSITION_SOUND. The Cortosis style parses byte for byte, runs the whole sequence on the simulator with nothing unsupported (USER1 -> tr00; clash -> tr01; FAST_OFF and retraction 300 ms later; tr02 and emitter sparks for 10 s; tr03 and a partial wipe; FAST_ON and ignition), and compiled on the real toolchain with the fett263 prop at 87% of V2 flash in 23 s. The permanent test is a control-loop style of the same shape written for the purpose.

> Prop files in the simulator, 2026-09-22: asked whether the demo room lets you pick the prop, since these styles lean on Fett263's. It did not, and the check showed why it must: from the prop sources, a style's EFFECT_FAST_OFF/ON turn the saber off and on only under the Fett263 prop (sa22c, BC and prop_base ignore them), special abilities (EFFECT_USER1..4) are gestures only Fett263 (with FETT263_SPECIAL_ABILITIES) and BC (1-8) have, and EFFECT_TRANSITION_SOUND plays tr sounds only on Fett263 and BC. Hiltwright's Fett263 config did not emit FETT263_SPECIAL_ABILITIES, so the Cortosis ability would have done nothing on hote2. Now: `PropBehaviour`/`PROPS` in core describe each prop's answers; `BladeSim.prop` gates whether a style can power the saber (the effect still goes on the bus for the style's own layers, as on the board); the demo room has a Prop file picker that defaults to the saber's prop ('(this saber)'), enables Ability 1-4 only where the prop has the gesture, says what the chosen prop does, and plays tr sounds only where the prop would; and the config generator adds FETT263_SPECIAL_ABILITIES when a used look names EFFECT_USER on a Fett263 saber (with a warning saying how the gesture goes) and warns that sa22c/default have no gesture for it. The 2D preview (editor, Looks, Presets) assumes Fett263. Tests cover the define, the warnings, and sa22c ignoring a style's FAST_OFF where Fett263 obeys it.

**The whole import is shelved as of 2026-09-20** while the owner gathers current Xenopixel and CFX references; the Xenopixel code stays in, unverified against a real card.

**Phase 1 — App shell + Tier 1 + Tier 3. Ship.** Electron shell, typed IPC, renderer board bridge. Connect, identify the board, preset manager, colour/argument editing with live preview, variation, volume, effect test buttons, battery. SD manager with font validation and naming conversion. `presets.ini` backup and restore. No toolchain yet.

**Phase 2 — Tier 2. Ship.** Toolchain manager, build engine with error translation, flash-budget meter, flash engine with backup and verification, Windows driver wizard, Linux udev guidance, macOS notarisation. Hardware wizard producing the `BladeConfig` identity.

**Phase 3 — Content and on-ramps.** Style editor and curated library per OS version (GPL, attributed), CFX and Xenopixel import, updater polish, ProffieOS release tracking.

### MVP scope (revised order)

1. Config model: parse and generate `config.h`, round-trip safe.
2. Runtime tier over the renderer board bridge: presets, colours, fonts, backups.
3. Toolchain manager: `arduino-cli` + core + GCC bootstrap with integrity checks.
4. One-click compile and flash with plain-language errors, flash meter, and pre-flash backup.
5. Preset/style library UI, curated per OS version, with live preview.
6. Update checker for ProffieOS versions and the app.

---

## 7. Risks

| Risk | Mitigation |
|---|---|
| Windows DFU driver is the ecosystem's top support burden | First-run wizard with elevated helper; detect the missing driver by the bootloader never enumerating; clear BOOT+RESET guidance |
| Wrong `BladeConfig` damages hardware | Identity rule in §5.3; wizard validates pins against the board profile; explicit confirmation on any change |
| Flash budget exceeded, worst on V2 | Meter before build; `opt=os`; style reuse via arguments; `DISABLE_DIAGNOSTIC_COMMANDS`, `DISABLE_TALKIE` suggestions |
| ProffieOS and style-language drift (OS7 / OS8 / master) | Pinned OS versions; per-version validator; CI compiles the curated library against each pinned OS |
| Long tail of hand-written configs | Opaque-text preservation in the codec; "import as text, edit what we understand" |
| Signing and notarisation cost | SignPath Foundation for Windows OSS signing; Apple Developer for notarisation; ship unsigned pre-release if needed |
| GPL obligations | Aggregation with notices; users compile their own firmware; source pointers in the SD `ProffieOS source/` folder |
| Community relations | Announce on The Crucible; attribution everywhere; never scrape fett263.com; contribute upstream where useful |

---

## 8. Measurements before Phase 2 design is frozen

1. ~~Cold and warm compile times~~ Measured on a fast desktop (see 06). Still needed on a mid-range and a low-end laptop.
2. Binary sizes for 15 and 30 distinct curated styles on V2 and V3, to calibrate the flash meter. Points so far (see 06): OS base with edit mode ≈ 195–200 KB; four current Fett263 library styles ≈ 216 KB (82% of V2, 43% of V3); ten OS6-era instantiations ≈ 227 KB. Distinct style instantiations drive size and compile time; argument reuse is free.
3. ~~Does the 1200-baud open/close reboot the board into DFU~~ Proven on Windows with .NET SerialPort against a V2.2 (see 06 section 7); still to confirm from Electron's Web Serial, on macOS/Linux, and on a V3.
4. ~~`dfu-util` behaviour and timing~~ Proven on Windows: 256 KB backup in 0.9 s, 214 KB write in 58 s, `:leave` reset and `version` check work. Still to confirm on macOS/Linux.
5. Mass-storage volume detection with `MOUNT_SD_SETTING` and the `sd` command; copy speed versus a card reader.
6. License terms of the `arduino-proffieboard` core for bundling. Driver helper decision made: build our own from libwdi's `wdi-simple` (LGPL v3; releases ship only Zadig, no CLI binary) and launch it elevated from the first-run wizard; the spike confirmed problem code 28 as the detection signal and that binding takes effect immediately with the board in bootloader mode.
7. How broadly ProffieConfig's parser handles hand-written configs, to set our import target.
